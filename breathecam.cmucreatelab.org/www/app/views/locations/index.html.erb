<% content_for :head do %>
  <script>
    var loc = "#s=" + "<%= @location_id %>"
    var locHash = location.hash ? "&" + location.hash.slice(1) : ""
    window.location = "http://breatheproject.org/learn/breathe-cam/" + loc + locHash;
  </script>
<% end %>
