# DO NOT EDIT THIS FILE - edit the master and reinstall.
# (/tmp/crontab.YO3zXG/crontab installed on Tue Jul  7 17:58:19 2015)
# (Cron version -- $Id: crontab.c,v 2.13 1994/01/17 03:20:37 vixie Exp $)
# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command

LOC_ID=shenango1
* * * * * . $HOME/.profile; ruby /home/pdille/breathecam/create_breathe_cam_tm.rb "/tm1/usr3/timemachine_uploads/guest_uploader/a7s_shenango1/050-original-images" "pdille@timemachine1.gc.cs.cmu.edu:/usr0/web/timemachines/ecam/timemachines/$LOC_ID" "/home/pdille/breathecam/$LOC_ID.pto" "$LOC_ID" -j 24 --symlink-input --rsync-location-json -incremental-update-interval 10 --skip-rotate --skip-stitch --skip-leader --skip-trailer -ssd-mount "/workspace" -working-dir "/workspace/$LOC_ID.tmc" -camera-type "ecam" --input-date-from-file --sort-by-exif-dates --append-inplace -future-appending-frames 17000 --skip-qtfaststart-append >> /home/pdille/breathecam/$LOC_ID.log 2>&1
 
# Every day at 4:00 AM
0 4 * * * . $HOME/.profile; ruby /home/pdille/breathecam/nightly_breathecam_rsync.rb "/workspace/$LOC_ID.tmc" "/timemachines/ecam/$LOC_ID" "pdille@timemachine1.gc.cs.cmu.edu" "/usr0/web/timemachines/ecam/timemachines/$LOC_ID" >> /home/pdille/breathecam/$LOC_ID-rsync.log 2>&1
 
# Check on startup if create_breathe_cam_tm.rb was killed mid-process and try to recover.
@reboot /home/pdille/breathecam/breathecam-startup-check
